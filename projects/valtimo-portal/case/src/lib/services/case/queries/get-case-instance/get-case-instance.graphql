query GetCaseInstance($id: UUID!) {
  getCaseInstance(id: $id) @namedClient(name: "portal-api") {
    caseDefinitionId
    createdOn
    externalId
    id
    submission
    status
    userId
  }
}
